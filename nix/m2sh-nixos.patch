diff --git a/config/home/profiles/m2sh.nix b/config/home/profiles/m2sh.nix
index e552a78339..92d4e10dc7 100644
--- a/config/home/profiles/m2sh.nix
+++ b/config/home/profiles/m2sh.nix
@@ -30,6 +30,7 @@
     pkgs.antigravity-hub
     # The M2 Air's notch, in physical pixels: rho-gui puts its status line at
     # the top and leaves this band in the middle empty. Width is tuned by eye.
+    # (The drv desktop's rho app gets the same through RHO_NOTCH in its manifest.)
     (pkgs.rho-gui-bin.overrideAttrs (old: {
       postFixup = (old.postFixup or "") + ''
         wrapProgram $out/bin/rho-gui --set-default RHO_NOTCH 290x56
diff --git a/config/system/niri-builder/default.nix b/config/system/niri-builder/default.nix
index 18cc4e601d..91e1f810e3 100644
--- a/config/system/niri-builder/default.nix
+++ b/config/system/niri-builder/default.nix
@@ -64,7 +64,7 @@ let
     ]);
     text = ''
       action="''${1:-}"
-      ref="''${2:-rho/gpu-process}"
+      ref="''${2:-rho/policy}"
 
       case "$action" in
         build|release) ;;
@@ -116,9 +116,14 @@ let
       cd "$repo"
       build_start="$SECONDS"
       # Same feature set as pkgs/niri.nix, spelled out so branches with
-      # different defaults produce the same binary.
+      # different defaults produce the same binary. The desktop's services
+      # (services.drv, nix/module.nix in the niri repo) build alongside: the
+      # same list as the repo's flake and nix/cross-drv.sh.
       cargo build --release --locked --target aarch64-unknown-linux-gnu \
-        --no-default-features --features dbus,systemd,xdp-gnome-screencast
+        --no-default-features \
+        --features niri/dbus,niri/systemd,niri/xdp-gnome-screencast,drv-portal/service \
+        -p niri -p drv-supervisor -p drv-appd -p drv-forker -p drv-init -p drv-bridge \
+        -p drv-seat -p drv-auth -p drv-lock -p drv-menu -p drv-portal -p drv-agent -p drv-keys
       build_seconds=$((SECONDS - build_start))
 
       printf '%s\t%s\t%s\n' \
@@ -131,13 +136,21 @@ let
         stage="$state/.stage"
         rm -rf "$stage"
         mkdir -p "$stage/bin"
-        # Line tables go to a companion file; the shipped binary keeps
-        # .symtab so backtraces on the device still have function names.
+        # Line tables go to a companion file (niri's; the services are small
+        # enough to keep theirs); every shipped binary keeps .symtab so
+        # backtraces on the device still have function names.
         debug_file="$state/niri_''${version}.debug"
         llvm-objcopy --only-keep-debug "$binaries/niri" "$debug_file"
         install -m 0755 "$binaries/niri" "$stage/bin/niri"
         llvm-objcopy --strip-debug \
           --add-gnu-debuglink="$debug_file" "$stage/bin/niri"
+        # By name, not by glob: the target directory keeps binaries of crates that no
+        # longer exist.
+        for b in drv-supervisor drv-appd drv-forker drv-init drv-bridge drv-seatd drv-authd \
+                 drv-lock drv-menu drv-portal drv-agent drv-keys chooser-probe cast-probe; do
+          llvm-objcopy --strip-debug "$binaries/$b" "$stage/bin/$b"
+          chmod 0755 "$stage/bin/$b"
+        done
         # Session script, systemd units, desktop file, portal config and the
         # default config ship raw; pkgs/niri-bin.nix installs them the same
         # way pkgs/niri.nix does from a source checkout.
diff --git a/flake.nix b/flake.nix
index c11bb91e2b..35a5bd7765 100644
--- a/flake.nix
+++ b/flake.nix
@@ -39,6 +39,8 @@
       flake = false;
     };
     transcriber.url = "sourcehut:~maan2003/transcriber";
+    niri.url = "github:maan2003/niri/rho/policy";
+    niri.inputs.nixpkgs.follows = "nixpkgs";
     ws-mgr = {
       url = "sourcehut:~maan2003/ws-mgr";
       flake = false;
diff --git a/hosts/m2sh.nix b/hosts/m2sh.nix
index f400c3329a..2ac69ad177 100644
--- a/hosts/m2sh.nix
+++ b/hosts/m2sh.nix
@@ -40,9 +40,13 @@ in
 {
   system.stateVersion = "24.05";
   # The compositor comes prebuilt from niri-builder (pkgs/niri-bin.nix) so this
-  # host never compiles niri; config/system/niri.nix and the Home Manager
-  # profile keep referring to pkgs.niri.
-  nixpkgs.overlays = [ (final: prev: { niri = final.niri-bin; }) ];
+  # host never compiles niri. Two of them: the stock one (the last niri-only archive) is
+  # pkgs.niri for greetd, config/system/niri.nix and the Home Manager profile; the drv
+  # desktop's (niri plus its services) runs on VT 7, in the background at boot.
+  nixpkgs.overlays = [ (final: prev: { niri = final.niri-bin-stock; }) ];
+  services.drv.package = pkgs.niri-bin;
+  # The system-wide PipeWire (and its PulseAudio server) of the drv module.
+  users.users.${config.users.mainUser}.extraGroups = [ "pipewire" ];
   system.replaceDependencies.replacements =
     securityReplacements pkgs
     ++ securityReplacements pkgs.pkgs-unstable
@@ -61,6 +65,10 @@ in
     # ../config/system/network/ivpn.nix
     ../config/system/asahi-sound.nix
     ../config/system/niri.nix
+    # The drv desktop (the set of services and the apps as manifests) next to
+    # greetd + niri-session; niri-bin ships all of its binaries.
+    inputs.niri.nixosModules.default
+    inputs.niri.nixosModules.m2sh
     ../config/system/memory.nix
     ../config/system/zram.nix
     # ../config/system/fs/encrypted-swap.nix
diff --git a/pkgs/niri-bin.nix b/pkgs/niri-bin.nix
index 68aa9c349b..ea42d06301 100644
--- a/pkgs/niri-bin.nix
+++ b/pkgs/niri-bin.nix
@@ -19,14 +19,24 @@
   seatd,
   systemd,
   wayland,
+  fuse3,
+  # Copy version and hash from the pin printed by `niri-build release`
+  # (see tasks/releasing-niri.md). The url is templated from the version.
+  version ? "20260922-g0fa3daeb15b9",
+  hash ? "sha256-S97AljZJ8PIQugm/FZ4rlpjNkj/joMwAbc0AOJDGegs=",
 }:
 
 stdenv.mkDerivation (finalAttrs: {
   pname = "niri-bin";
-  # Copy version and hash from the pin printed by `niri-build release`
-  # (see tasks/releasing-niri.md). The url is templated from the version.
-  version = "20260917-g9fa3ca81b598";
+  inherit version;
   # Rollbacks (swap version and hash):
+  #   the drv desktop before it could sit beside greetd (no services.drv.homeVt):
+  #     version = "20260922-g5e6dca59ec06";
+  #     hash = "sha256-XAGcXmJvCgpiLtCy7Kx6Az8Z+MY0H+Stny5v4z1LFVM=";
+  #   the last niri-only archive (rho/gpu-process), before the drv desktop's binaries
+  #   shipped; niri-bin-stock (pkgs/overlay.nix) pins it for the host's own session:
+  #     version = "20260917-g9fa3ca81b598";
+  #     hash = "sha256-HyUbtMgZ+oj82AcIVIPyWyfM3SLjow6S7P8KigcuhXw=";
   #   self-sealing GPU process, still with NIRI_GPU_THREAD (protocol v15):
   #     version = "20260917-g8fe2bae74aa8";
   #     hash = "sha256-x0nLn4q44VI8h6OVOwQWw1iLwAqBZtqJpYEJNsVVKfI=";
@@ -53,7 +63,7 @@ stdenv.mkDerivation (finalAttrs: {
 
   src = fetchurl {
     url = "https://pub-c02578899b644007b0b5aae08b7e73b3.r2.dev/niri/${finalAttrs.version}/aarch64-linux/niri_${finalAttrs.version}_arm64.tar.zst";
-    hash = "sha256-HyUbtMgZ+oj82AcIVIPyWyfM3SLjow6S7P8KigcuhXw=";
+    inherit hash;
   };
 
   sourceRoot = ".";
@@ -92,6 +102,7 @@ stdenv.mkDerivation (finalAttrs: {
     seatd
     systemd # Includes libudev
     wayland # For libwayland-client
+    fuse3 # drv-portal's documents mount
   ];
 
   postPatch = ''
@@ -102,7 +113,9 @@ stdenv.mkDerivation (finalAttrs: {
 
   installPhase = ''
     runHook preInstall
-    install -Dm0755 bin/niri -t $out/bin
+    # niri and the desktop's services (drv-supervisor, drv-appd, drv-forker, drv-init,
+    # drv-bridge, drv-seatd, drv-authd, drv-lock, drv-menu, drv-portal, drv-agent, drv-keys).
+    install -Dm0755 bin/* -t $out/bin
     install -Dm0644 resources/README.md resources/default-config.kdl -t $doc/share/doc/niri
     install -Dm0644 resources/niri.desktop -t $out/share/wayland-sessions
     install -Dm0644 resources/niri-portals.conf -t $out/share/xdg-desktop-portal
diff --git a/pkgs/overlay.nix b/pkgs/overlay.nix
index 30b4267dcf..5e758ce242 100644
--- a/pkgs/overlay.nix
+++ b/pkgs/overlay.nix
@@ -119,6 +119,11 @@ in
   ia-fonts = self.callPackage ./ia-fonts.nix { };
   niri = self.callPackage ./niri.nix { };
   niri-bin = self.callPackage ./niri-bin.nix { };
+  # The last niri-only build (rho/gpu-process): the host's own session under greetd.
+  niri-bin-stock = self.callPackage ./niri-bin.nix {
+    version = "20260917-g9fa3ca81b598";
+    hash = "sha256-HyUbtMgZ+oj82AcIVIPyWyfM3SLjow6S7P8KigcuhXw=";
+  };
   nsproxy = self.callPackage ./nsproxy.nix { };
   parent-ns-enter = self.callPackage ./parent-ns-enter { };
   trace_processor = self.callPackage ./trace-processor.nix { };
