[package]
name = "drv-lock"
version = "26.4.0"
edition = "2024"
license = "GPL-3.0-or-later"
description = "The lock screen: draws itself and takes the PIN; drv-authd does the unlocking."

[dependencies]
drv-auth = { version = "26.4.0", path = "../drv-auth" }
pangocairo = "0.21.5"
smithay-client-toolkit = "0.21.1"
wayland-client = "0.31.15"
zeroize = "1"
