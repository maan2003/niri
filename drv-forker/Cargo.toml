[package]
name = "drv-forker"
version.workspace = true
description = "drv-appd's privileged helper: forks one sandboxed app per request, nothing else"
authors.workspace = true
license.workspace = true
edition.workspace = true
repository.workspace = true

[dependencies]
clap.workspace = true
drv-os = { version = "26.4.0", path = "../drv-os" }
drv-policy = { version = "26.4.0", path = "../drv-policy" }
libc = "0.2"
seccompiler = "0.5"
rustix = { version = "1.1.4", features = ["event", "fs", "mount", "net", "pipe", "process", "std", "thread"] }

[[bin]]
name = "drv-forker"
path = "src/main.rs"
