[package]
name = "drv-menu"
version = "26.4.0"
edition = "2024"
license = "GPL-3.0-or-later"
description = "The app menu: lists what drv-appd can launch and launches the pick by name."

[dependencies]
drv-os = { version = "26.4.0", path = "../drv-os" }
drv-policy = { version = "26.4.0", path = "../drv-policy" }
drv-ui = { version = "26.4.0", path = "../drv-ui" }
